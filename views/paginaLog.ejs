<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logs - Guerreiro NEWS</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="d-flex flex-column min-vh-100" >
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/guerreironews">GUERREIRO NEWS</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/guerreironews">Início</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/guerreironews/todas">Todas as Notícias</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/guerreironews/criarNoticia">Criar Notícia</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/guerreironews/logs">Log de Notícias</a>
                    </li>

                </ul>
            </div>
        </div>
    </nav>

    <section class="py-5">
        <div class="container">
            <h1 class="display-4">Logs de Notícias</h1>
            

            <form action='/guerreironews/excluirlogs' method="POST" class="d-inline">
                <button type="submit" class="btn btn-danger btn-sm">Excluir Todos os Logs</button>
            </form>

            <ul class="list-group mt-4">
                <% if (logs.length > 0) { %>
                    <% logs.forEach(log => { %>
                        <li class="list-group-item">
                            <strong>Tipo:</strong> <%= log.tipo %> <br>
                            <strong>Descrição:</strong> <%= log.descricao %> <br>
                            <small><strong>Data:</strong> <%= log.data %></small>
                        </li>
                    <% }); %>
                <% } else { %>
                    <li class="list-group-item text-center">Nenhum log registrado no momento.</li>
                <% } %>
            </ul>
        </div>
    </section>

    <footer class="bg-dark text-light py-4 mt-auto">
        <div class="container text-center">
            <p>2025 Guerreiro NEWS. Marca Registrada!</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
